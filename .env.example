NODE_ENV=development
PORT=3000
APP_URL=http://localhost:3000

DATABASE_URL=postgresql://user:password@localhost:5432/cryptorsi
JWT_SECRET=change_me_in_production

BINANCE_ENV=demo
# Binance API credentials are managed from the web UI (stored encrypted in DB)

ALLOW_LIVE_TRADING=false
LOG_LEVEL=info
APP_ENCRYPTION_KEY=
